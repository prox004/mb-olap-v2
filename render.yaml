# Render Web Service Configuration (Root Directory: empty / project root)
services:
  - type: web
    name: mbaazar-analytics-backend
    env: python
    region: singapore
    buildCommand: "pip install -r backend/requirements.txt"
    startCommand: "python -m uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: DUCKDB_PATH
        value: backend/db/olap_warehouse.duckdb
      - key: PYTHONPATH
        value: .
